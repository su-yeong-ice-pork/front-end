import {StyleSheet} from "react-native";

export const ProfileActionStyles = StyleSheet.create({
  buttonContainer: {
    flex: 1,
    justifyContent: 'flex-start',
    alignItems: 'stretch',
  },
  separator: {
    height: 1,
    width: '100%',
    backgroundColor: '#DBDBDB',
  },
});