import {StyleSheet} from 'react-native-css-interop';

export const ProfileStyles = StyleSheet.create({
  profileImage: {
    position: 'relative',
    width: 80,
    height: 80,
    marginLeft: 40,
  },
});
